{
  "sqlist":[
    "CREATE TABLE if not exists todolist(id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT, timestamp FLOAT, done boolean DEFAULT false, todo TEXT, category_id INTEGER)",
	  "CREATE TABLE if not exists category(id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT, name TEXT, timestamp FLOAT)"
  ],
  "saveToFolder": "./entity",
  "options": {
  	"classFormatter": "{0}Model",
    "mapping":{
      "todolist": {
        "timestamp": "date"
      },
      "category": {
        "timestamp": "date"
      }
    }
  }
}
